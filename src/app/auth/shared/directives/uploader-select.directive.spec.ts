import { UploaderSelectDirective } from './uploader-select.directive';

describe('UploaderSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new UploaderSelectDirective();
    expect(directive).toBeTruthy();
  });
});
