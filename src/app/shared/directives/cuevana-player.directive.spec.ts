import { CuevanaPlayerDirective } from './cuevana-player.directive';

describe('CuevanaPlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new CuevanaPlayerDirective();
    expect(directive).toBeTruthy();
  });
});
